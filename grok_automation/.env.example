# Chrome DevTools Protocol endpoint
# Launch Chrome with launch_chrome.bat first, then this URL points to it.
CDP_URL=http://localhost:9222

# Default output directory for generated images/videos
OUTPUT_DIR=./output
